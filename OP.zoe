MODULE op
--
-- TEST OPERATORS
--
WHERE


-- UNARY

u1 = (-7);
u2 = (!FALSE);
u3 = (SHOW [1,2,3,4]);

-- PRECENDENCE AND LOGICAL

l1 x = x <= 5 || x >= 7;
l2 = TRUE || TRUE && FALSE;

-- LIST INDEXING

i1 = [1,2,3,4,5,6] !! 3;

-- NAMED TUPLE

namedTuple = {a = 1; b = 2; c = (\ x y -> x + y)};

selector1 = namedTuple >> a;
selector2 = namedTuple >> b;

newTup = namedTuple << {a = 4}; 